echo "# Time-table-matcher

This is a React app that helps match timetables based on selected preferences.

## Run locally
npm install  
npm start
" > README.md

git add README.md
git commit -m "Add README"
git push
